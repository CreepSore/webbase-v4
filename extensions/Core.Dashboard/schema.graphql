type Log {
    id: ID!
    date: String!
    level: String
    infos: [String]
    message: String
}

type DashboardPage {
    id: String!
    name: String!
    href: String!
    neededPermissions: [String]
}

type Query {
    logs: [Log]
    pages: [DashboardPage]
}
